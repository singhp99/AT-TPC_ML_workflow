## Data Limitation

The model we are hoping to train a classification model, hence it is important to balance the classes for the purposes of avoid bias. We can do so simply by ensuring that each class has the same amount of training examples&mdash;by limiting them with the class with the lowest events. 

::: scripts.ml_preprocessing_steps.DataLimitation
